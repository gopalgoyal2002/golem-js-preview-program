import { AbstractAggregator, ItemInfo } from './abstract_aggregator.js';
export interface TimesInfo extends ItemInfo {
    startTime: number;
    stopTime: number;
    duration: number;
}
interface Payload {
    id: string;
    startTime: number;
    stopTime?: number;
}
export declare class Times extends AbstractAggregator<Payload, TimesInfo> {
    beforeAdd({ id, startTime, stopTime }: Payload): TimesInfo;
    stop({ id, stopTime }: {
        id: any;
        stopTime: any;
    }): void;
}
export {};
