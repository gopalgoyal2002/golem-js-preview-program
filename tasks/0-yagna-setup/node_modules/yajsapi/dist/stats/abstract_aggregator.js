import collect from "collect.js/dist/index.js";
export class AbstractAggregator {
    items = new Map();
    add(event) {
        const item = this.beforeAdd(event);
        this.items.set(item.id, item);
    }
    getById(id) {
        return this.items.get(id);
    }
    getByField(field, value) {
        return this.getAll().where(field, "==", value);
    }
    updateItemInfo(id, data) {
        const item = this.items.get(id);
        if (!item)
            return;
        this.items?.set(id, {
            ...item,
            ...data,
        });
    }
    getAll() {
        return collect([...this.items.values()]);
    }
}
//# sourceMappingURL=abstract_aggregator.js.map