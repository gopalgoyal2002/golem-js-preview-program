import { AbstractAggregator } from './abstract_aggregator.js';
export interface InvoiceInfo {
    id: string;
    providerId: string;
    agreementId: string;
    amount: number;
}
interface Payload {
    id: string;
    providerId: string;
    agreementId: string;
    amount: string;
}
export declare class Invoices extends AbstractAggregator<Payload, InvoiceInfo> {
    beforeAdd(payload: any): InvoiceInfo;
    getByProviderId(providerId: string): Collection<R>;
    getByAgreementId(agreementId: string): Collection<R>;
}
export {};
