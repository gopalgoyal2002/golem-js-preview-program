import { Package } from '../package/index.js';
import { Allocation } from '../payment/index.js';
import { Demand, DemandOptions } from './demand.js';
export declare class DemandFactory {
    private taskPackage;
    private allocations;
    private options;
    constructor(taskPackage: Package, allocations: Allocation[], options?: DemandOptions);
    create(): Promise<Demand>;
    private getDecorations;
    private getBaseDecorations;
}
