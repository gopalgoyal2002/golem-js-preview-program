import { Proposal as ProposalModel } from "ya-ts-client/dist/ya-market/src/models/index.js";
import { RequestorApi } from "ya-ts-client/dist/ya-market/api.js";
import { DemandOfferBase } from "ya-ts-client/dist/ya-market/index.js";
export declare class Proposal {
    private readonly subscriptionId;
    private readonly api;
    private readonly demandRequest;
    private eventTarget?;
    readonly id: string;
    readonly issuerId: string;
    readonly properties: object;
    readonly constraints: string;
    readonly timestamp: string;
    private readonly state;
    private readonly prevProposalId;
    private _score;
    constructor(subscriptionId: string, api: RequestorApi, model: ProposalModel, demandRequest: DemandOfferBase, eventTarget?: EventTarget | undefined);
    set score(score: number | null);
    get score(): number | null;
    isInitial(): boolean;
    isDraft(): boolean;
    isExpired(): boolean;
    isRejected(): boolean;
    reject(reason?: string): Promise<void>;
    respond(chosenPlatform: string): Promise<void>;
}
