import { NetworkOptions } from './network.js';
import { NetworkNode } from './node.js';
export type NetworkServiceOptions = Omit<NetworkOptions, "networkOwnerId">;
export declare class NetworkService {
    private options?;
    private network?;
    private logger?;
    constructor(options?: NetworkServiceOptions | undefined);
    run(networkOwnerId?: string): Promise<void>;
    addNode(nodeId: string, ip?: string): Promise<NetworkNode>;
    end(): Promise<void>;
}
