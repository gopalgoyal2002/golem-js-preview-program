export interface QueueableTask {
    isQueueable(): boolean;
}
export declare class TaskQueue<Task extends QueueableTask> {
    protected itemsStack: Array<Task>;
    addToEnd(task: Task): void;
    addToBegin(task: Task): void;
    get size(): number;
    get(): Task | undefined;
    private _checkIfTaskIsEligibleForAdd;
}
