/// <reference types="node" />
import { StorageProvider } from "./provider.js";
import { Logger } from "../utils/index.js";
export declare class GftpStorageProvider implements StorageProvider {
    private logger?;
    private gftpServerProcess;
    private reader;
    private publishedUrls;
    constructor(logger?: Logger | undefined);
    init(): Promise<void>;
    isInitiated(): boolean;
    private generateTempFileName;
    private getGftpServerProcess;
    receive(path: string): Promise<string>;
    publish(src: string | Buffer): Promise<string>;
    release(urls: string[]): Promise<void>;
    close(): Promise<void>;
    private jsonrpc;
    readStream(readable: any): AsyncGenerator<string, void, unknown>;
    private uploadStream;
    private uploadBytes;
    private uploadFile;
}
