import { DemandOptions } from "./demand.js";
import { RequestorApi } from "ya-ts-client/dist/ya-market/api.js";
import { Logger } from "../utils/index.js";
import { MarketOptions } from "./service.js";
import { YagnaOptions } from "../executor/index.js";
export declare class DemandConfig {
    readonly api: RequestorApi;
    readonly yagnaOptions?: YagnaOptions;
    readonly timeout: number;
    readonly expiration: number;
    readonly subnetTag: string;
    readonly maxOfferEvents: number;
    readonly offerFetchingInterval: number;
    readonly proposalTimeout?: number;
    readonly logger?: Logger;
    readonly eventTarget?: EventTarget;
    constructor(options?: DemandOptions);
}
export declare class MarketConfig extends DemandConfig {
    readonly debitNotesAcceptanceTimeout: number;
    constructor(options?: MarketOptions);
}
