import { Allocation as Model, MarketDecoration } from "ya-ts-client/dist/ya-payment/index.js";
import { AllocationConfig, BasePaymentOptions } from "./config.js";
export interface AllocationOptions extends BasePaymentOptions {
    account: {
        address: string;
        platform: string;
    };
    expires?: number;
}
export declare class Allocation {
    private options;
    readonly id: string;
    readonly timestamp: string;
    readonly timeout?: string;
    readonly address: string;
    readonly paymentPlatform: string;
    readonly totalAmount: string;
    private spentAmount;
    private remainingAmount;
    static create(options: AllocationOptions): Promise<Allocation>;
    constructor(options: AllocationConfig, model: Model);
    getRemainingAmount(): Promise<string>;
    getSpentAmount(): Promise<string>;
    release(): Promise<void>;
    getDemandDecoration(): Promise<MarketDecoration>;
    private refresh;
}
