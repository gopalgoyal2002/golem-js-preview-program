import { Account } from "ya-ts-client/dist/ya-payment/src/models";
import { BasePaymentOptions } from "./config.js";
export interface AccountsOptions extends Omit<BasePaymentOptions, "yagnaOptions"> {
    yagnaOptions?: {
        apiKey: string;
        basePath?: string;
    };
}
export declare class Accounts {
    private options;
    static create(options?: AccountsOptions): Promise<Accounts>;
    private constructor();
    list(): Promise<Account[]>;
}
