import { BasePaymentOptions, InvoiceConfig } from './config.js';
import { DebitNote as Model } from "ya-ts-client/dist/ya-payment/src/models";
import { BaseNote } from './invoice.js';
import { Rejection } from './rejection.js';
export type InvoiceOptions = BasePaymentOptions;
export declare class DebitNote extends BaseNote<Model> {
    protected options: InvoiceConfig;
    readonly id: string;
    readonly previousDebitNoteId?: string;
    readonly timestamp: string;
    readonly activityId: string;
    readonly totalAmountDue: string;
    readonly usageCounterVector?: object;
    static create(debitNoteId: string, options?: InvoiceOptions): Promise<DebitNote>;
    protected constructor(model: Model, options: InvoiceConfig);
    accept(totalAmountAccepted: string, allocationId: string): Promise<void>;
    reject(rejection: Rejection): Promise<void>;
    protected refreshStatus(): Promise<void>;
}
