import { Activity, ActivityOptions } from './activity.js';
export declare class ActivityFactory {
    private readonly agreementId;
    private readonly options;
    constructor(agreementId: string, options?: ActivityOptions);
    create(secure?: boolean): Promise<Activity>;
    private createActivity;
}
