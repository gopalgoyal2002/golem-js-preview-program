import { TaskOptions } from "./service.js";
import { ActivityConfig } from "../activity/config.js";
import { Logger } from "../utils/index.js";
import { StorageProvider } from "../storage/index.js";
export declare class TaskConfig extends ActivityConfig {
    readonly maxParallelTasks: number;
    readonly taskRunningInterval: number;
    readonly taskTimeout: number;
    readonly activityStateCheckingInterval: number;
    readonly storageProvider?: StorageProvider;
    readonly logger?: Logger;
    constructor(options?: TaskOptions);
}
