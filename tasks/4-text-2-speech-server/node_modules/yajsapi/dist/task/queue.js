export class TaskQueue {
    itemsStack = [];
    addToEnd(task) {
        this._checkIfTaskIsEligibleForAdd(task);
        this.itemsStack.push(task);
    }
    addToBegin(task) {
        this._checkIfTaskIsEligibleForAdd(task);
        this.itemsStack.unshift(task);
    }
    get size() {
        return this.itemsStack.length;
    }
    get() {
        return this.itemsStack.shift();
    }
    _checkIfTaskIsEligibleForAdd(task) {
        if (!task.isQueueable())
            throw new Error("You cannot add a task that is not in the correct state");
    }
}
//# sourceMappingURL=queue.js.map