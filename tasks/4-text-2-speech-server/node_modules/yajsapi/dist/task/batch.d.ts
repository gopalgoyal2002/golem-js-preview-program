/// <reference types="node" />
import { Activity, Result } from "../activity/index.js";
import { StorageProvider } from "../storage/provider.js";
import { Logger } from "../utils/index.js";
import { Readable } from "stream";
export declare class Batch {
    private activity;
    private storageProvider?;
    private logger?;
    private script;
    static create(activity: Activity, storageProvider?: StorageProvider, logger?: Logger): Batch;
    constructor(activity: Activity, storageProvider?: StorageProvider | undefined, logger?: Logger | undefined);
    run(...args: Array<string | string[]>): this;
    uploadFile(src: string, dst: string): this;
    uploadJson(json: object, dst: string): this;
    downloadFile(src: string, dst: string): this;
    end(): Promise<Result[]>;
    endStream(): Promise<Readable>;
}
