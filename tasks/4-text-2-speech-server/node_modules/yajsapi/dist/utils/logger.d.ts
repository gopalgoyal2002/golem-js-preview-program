export declare enum LogLevel {
    debug = "debug",
    info = "info",
    warn = "warn",
    error = "error",
    log = "log"
}
export interface Logger {
    level: string;
    setLevel(level: string): any;
    log(msg: any): any;
    info(msg: any): any;
    warn(msg: any): any;
    error(msg: any): any;
    debug(msg: any): any;
    table?(object: any): any;
}
