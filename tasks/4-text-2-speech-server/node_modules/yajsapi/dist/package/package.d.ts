import { MarketDecoration } from "../market/builder.js";
import { Logger } from "../utils/index.js";
export interface PackageOptions {
    engine?: string;
    minMemGib?: number;
    minStorageGib?: number;
    minCpuThreads?: number;
    minCpuCores?: number;
    capabilities?: string[];
    imageHash: string;
    repoUrl?: string;
    logger?: Logger;
}
export declare class Package {
    private options;
    private logger?;
    private constructor();
    static create(options: PackageOptions): Package;
    getDemandDecoration(): Promise<MarketDecoration>;
    private getRepoUrl;
    private resolveTaskPackageUrl;
}
