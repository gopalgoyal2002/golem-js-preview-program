import { Logger } from "../utils/index.js";
export interface RepoResolverOptions {
    logger?: Logger;
}
export declare class RepoResolver {
    private logger?;
    private constructor();
    static create({ logger }: RepoResolverOptions): RepoResolver;
    private isRecordValid;
    resolveRepoUrl(): Promise<any>;
    getRepoUrl(): Promise<any>;
    private resolveRepoUrlForBrowser;
    private resolveRepoUrlForNode;
}
