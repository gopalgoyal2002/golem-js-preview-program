import { AbstractAggregator } from './abstract_aggregator.js';
export class Providers extends AbstractAggregator {
    beforeAdd(payload) {
        if (payload.providerName) {
            return payload;
        }
        const provider = this.getById(payload.id);
        return {
            id: payload.id,
            providerName: provider?.providerName || "unknown",
        };
    }
}
//# sourceMappingURL=providers.js.map