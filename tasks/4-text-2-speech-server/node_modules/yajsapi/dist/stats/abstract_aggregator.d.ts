import { Collection } from "collect.js/dist/index.js";
export interface ItemInfo {
    id: string;
}
export declare abstract class AbstractAggregator<T, R extends ItemInfo> {
    protected items: Map<string, R>;
    add(event: T): void;
    abstract beforeAdd(item: T): R;
    getById(id: string): R | undefined;
    protected getByField(field: string, value: string | number): Collection<R>;
    protected updateItemInfo(id: string, data: any): void;
    getAll(): Collection<R>;
}
