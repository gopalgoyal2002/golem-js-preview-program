import { AbstractAggregator } from './abstract_aggregator.js';
export interface ActivityInfo {
    id: string;
    agreementId: string;
    taskId: string;
}
interface Payload {
    id: string;
    taskId: string;
    agreementId: string;
}
export declare class Activities extends AbstractAggregator<Payload, ActivityInfo> {
    beforeAdd(payload: any): ActivityInfo;
    getByTaskId(taskId: string): Collection<R>;
    getByAgreementId(agreementId: string): Collection<R>;
}
export {};
