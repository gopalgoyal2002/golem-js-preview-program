import { AbstractAggregator } from './abstract_aggregator.js';
export declare enum AgreementStatusEnum {
    Pending = "pending",
    Confirmed = "confirmed",
    Rejected = "rejected"
}
export interface AgreementInfo {
    id: string;
    providerId: string;
    proposalId: string;
    status: AgreementStatusEnum;
}
interface Payload {
    id: string;
    providerId: string;
    proposalId: string;
}
export declare class Agreements extends AbstractAggregator<Payload, AgreementInfo> {
    beforeAdd(payload: any): AgreementInfo;
    confirm(id: string): void;
    reject(id: string): void;
    getByProviderId(providerId: string): Collection<R>;
    getByProposalId(proposalId: string): any;
    getByStatus(status: AgreementStatusEnum): Collection<R>;
}
export {};
