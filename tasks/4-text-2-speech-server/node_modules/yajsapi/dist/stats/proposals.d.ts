import { AbstractAggregator } from './abstract_aggregator.js';
export interface ProposalInfo {
    id: string;
    providerId: string;
}
interface Payload {
    id: string;
    providerId: string;
}
export declare class Proposals extends AbstractAggregator<Payload, ProposalInfo> {
    beforeAdd(payload: any): ProposalInfo;
    getByProviderId(providerId: string): Collection<R>;
}
export {};
