import { AbstractAggregator } from './abstract_aggregator.js';
export interface PaymentInfo {
    id: string;
    providerId: string;
    agreementId: string;
    amount: number;
}
interface Payload {
    id: string;
    providerId: string;
    agreementId: string;
    amount: string;
}
export declare class Payments extends AbstractAggregator<Payload, PaymentInfo> {
    beforeAdd(payload: any): PaymentInfo;
    getByProviderId(providerId: string): Collection<R>;
    getByAgreementId(agreementId: string): Collection<R>;
}
export {};
